name: Cross-Platform CI

on:
  push:
    branches: ["main", "master"]
  pull_request:

jobs:
  validate:
    name: Validate on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        if: ${{ hashFiles('**/requirements*.txt', '**/pyproject.toml', '**/setup.py') != '' }}
        uses: actions/setup-python@v7
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        if: ${{ hashFiles('**/requirements*.txt', '**/pyproject.toml', '**/setup.py') != '' }}
        shell: bash
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt 2>/dev/null || true; fi
          if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt 2>/dev/null || true; fi

      - name: Run Python checks
        if: ${{ hashFiles('**/requirements*.txt', '**/pyproject.toml', '**/setup.py') != '' }}
        shell: bash
        run: |
          if [ -d tests ] || [ -d test ]; then
            python -m pytest
          else
            python -m compileall -q .
          fi

      - name: Setup Node
        if: ${{ hashFiles('**/package.json') != '' }}
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Node dependencies
        if: ${{ hashFiles('**/package.json') != '' }}
        shell: bash
        run: |
          for f in $(find . -name "package.json" -not -path "*/node_modules/*" 2>/dev/null); do
            dir=$(dirname "$f")
            (cd "$dir" && if [ -f package-lock.json ]; then npm ci 2>/dev/null; else npm install 2>/dev/null; fi) || true
          done

      - name: Run Node checks
        if: ${{ hashFiles('**/package.json') != '' }}
        shell: bash
        run: |
          npm run lint --if-present
          npm test --if-present
          npm run build --if-present

      - name: Setup Go
        if: ${{ hashFiles('**/go.mod') != '' }}
        uses: actions/setup-go@v5
        with:
          go-version: 'stable'

      - name: Run Go tests
        if: ${{ hashFiles('**/go.mod') != '' }}
        run: go test ./...

      - name: Setup Rust
        if: ${{ hashFiles('**/Cargo.toml') != '' }}
        uses: dtolnay/rust-toolchain@stable

      - name: Run Rust tests
        if: ${{ hashFiles('**/Cargo.toml') != '' }}
        shell: bash
        run: cargo test --workspace --all-features || cargo test
